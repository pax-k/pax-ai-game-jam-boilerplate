# Multiplayer architecture

The starter provides one Colyseus room with authoritative directional movement. Keep client input, server simulation, synchronized state, reconnection policy, room identity, and persistence choices explicit as the game grows.
